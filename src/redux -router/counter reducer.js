const initialstate={
    count:0
}

function Counterreducer(state=initialstate,action){
    if(action.type==="INC"){
       
    }
    if(action.type==="DEC"){
       
    }
    return state
}

export default Counterreducer